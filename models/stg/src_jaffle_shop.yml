version: 2

sources:
  - name: jaffle_shop
    database: development
    schema: "{{ env_var('DBT_JAFFLE_SHOP_SCHEMA') }}"
    tables:
      - name: orders
        freshness: 
          warn_after: {count: 6, period: hour}
        loaded_at_field: _etl_loaded_at
      - name: customers